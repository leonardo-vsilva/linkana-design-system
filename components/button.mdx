---
title: "Button"
description: "Elemento de ação primária. Dispara uma ação quando clicado — submit de form, abertura de modal, execução de operação."
---

## Variantes

<div style={{padding: '24px', background: '#fafafa', border: '1px solid #e5e5e5', borderRadius: '8px', display: 'flex', flexWrap: 'wrap', gap: '8px', alignItems: 'center'}}>
  <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: '#1a1a1a', color: '#fafafa', border: 'none', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>Default</button>
  <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: '#f5f5f5', color: '#1a1a1a', border: 'none', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>Secondary</button>
  <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: '#dc2626', color: '#fff', border: 'none', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>Destructive</button>
  <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: 'transparent', color: '#1a1a1a', border: '1px solid #e5e5e5', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>Outline</button>
  <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: 'transparent', color: '#1a1a1a', border: 'none', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>Ghost</button>
  <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: 'transparent', color: '#1a1a1a', border: 'none', fontSize: '14px', fontWeight: 500, cursor: 'pointer', textDecoration: 'underline', textUnderlineOffset: '4px', fontFamily: 'inherit'}}>Link</button>
</div>

| Variante | Ênfase | Uso |
|----------|--------|-----|
| `default` | Alta | Ação principal do fluxo — salvar, confirmar, publicar |
| `secondary` | Média | Ação alternativa válida — salvar rascunho, duplicar, visualizar |
| `destructive` | Alta | Ação irreversível — deletar, remover |
| `outline` | Baixa | Ação secundária visível — editar, filtros avançados, exportar, cancelar, voltar |
| `ghost` | Mínima | Ação contextual mínima — ícones de ação em hover, botões em contextos muito densos |
| `link` | Mínima | Ação que parece link de texto |

## Hierarquia e ordenamento

Botões comunicam prioridade visual. Cada variante sinaliza ao usuário qual ação é mais importante — por isso a combinação e o ordenamento importam tanto quanto a escolha individual.

### Princípio base

> **A ação de menor compromisso fica à esquerda. A ação de maior compromisso fica à direita.**

O olho lê da esquerda para direita. O usuário chega ao botão primário por último — depois de já ter lido todas as opções.

### Frequência por grupo de ações

| Variante | Qtd. por grupo | Posição | Representa |
|----------|---------------|---------|------------|
| `default` | 1 | Direita | "Confirme esta ação" |
| `secondary` | 0–1 | Centro | "Há uma alternativa válida" |
| `outline` | 0–2+ | Esquerda / Toolbar | "Ação secundária que precisa ser descobrível" |
| `ghost` | Ilimitado | Fora do grupo principal | "Ação mínima que não deve chamar atenção" |

<Note>
`outline` cobre dois papéis: ações de saída em grupos de decisão (cancelar, voltar) **e** ações secundárias visíveis em toolbars (editar, filtros avançados, exportar). O `ghost` é reservado para ações verdadeiramente discretas — geralmente `size="icon"` em hover ou contextos muito densos.
</Note>

### Padrões por contexto

| Contexto | Ordenamento (esq → dir) |
|----------|------------------------|
| Modal de confirmação | `Outline: Cancelar` → `Default: Confirmar` |
| Modal destrutivo | `Outline: Cancelar` → `Destructive: Excluir` |
| Formulário com rascunho | `Outline: Cancelar` → `Secondary: Salvar rascunho` → `Default: Publicar` |
| Wizard (step a step) | `Outline: Voltar` → `Default: Próximo` |
| Toolbar de listagem | `Outline: Filtros avançados` · `Outline: Exportar` · ... → `Default: Novo item` |
| Ação em linha (tabela/card) | `Outline: Editar` · `Outline: Duplicar` (sem grupo formal) |
| Ação de hover em campo/linha | `Ghost: ✏️` (ícone, aparece no hover) |

### Combinações válidas

<div style={{padding: '24px', background: '#fafafa', border: '1px solid #e5e5e5', borderRadius: '8px', display: 'flex', flexDirection: 'column', gap: '16px'}}>
  <div style={{display: 'flex', gap: '8px', alignItems: 'center'}}>
    <span style={{fontSize: '12px', color: '#16a34a', fontWeight: 600, width: '20px'}}>✅</span>
    <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: 'transparent', color: '#1a1a1a', border: '1px solid #e5e5e5', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>Cancelar</button>
    <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: '#1a1a1a', color: '#fafafa', border: 'none', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>Salvar</button>
    <span style={{fontSize: '12px', color: '#737373', marginLeft: '8px'}}>Outline → Default</span>
  </div>
  <div style={{display: 'flex', gap: '8px', alignItems: 'center'}}>
    <span style={{fontSize: '12px', color: '#16a34a', fontWeight: 600, width: '20px'}}>✅</span>
    <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: 'transparent', color: '#1a1a1a', border: '1px solid #e5e5e5', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>Cancelar</button>
    <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: '#f5f5f5', color: '#1a1a1a', border: 'none', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>Salvar rascunho</button>
    <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: '#1a1a1a', color: '#fafafa', border: 'none', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>Publicar</button>
    <span style={{fontSize: '12px', color: '#737373', marginLeft: '8px'}}>Outline → Secondary → Default</span>
  </div>
  <div style={{display: 'flex', gap: '8px', alignItems: 'center'}}>
    <span style={{fontSize: '12px', color: '#dc2626', fontWeight: 600, width: '20px'}}>❌</span>
    <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: '#1a1a1a', color: '#fafafa', border: 'none', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>Salvar</button>
    <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: '#1a1a1a', color: '#fafafa', border: 'none', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>Publicar</button>
    <span style={{fontSize: '12px', color: '#737373', marginLeft: '8px'}}>Dois Default concorrem — nenhum é primário</span>
  </div>
  <div style={{display: 'flex', gap: '8px', alignItems: 'center'}}>
    <span style={{fontSize: '12px', color: '#dc2626', fontWeight: 600, width: '20px'}}>❌</span>
    <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: '#1a1a1a', color: '#fafafa', border: 'none', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>Confirmar</button>
    <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: 'transparent', color: '#1a1a1a', border: '1px solid #e5e5e5', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>Cancelar</button>
    <span style={{fontSize: '12px', color: '#737373', marginLeft: '8px'}}>Default à esquerda — ordem invertida</span>
  </div>
</div>

<br />

> Acima de 3 botões na mesma linha, consolide as ações secundárias em um menu dropdown — múltiplos botões paralelos indicam que a arquitetura de informação precisa ser revisada.

### Outline — ações secundárias visíveis

O `outline` cobre ações que precisam ser descobríveis mas não competem com o CTA principal. Isso inclui tanto ações de saída em grupos de decisão quanto ações funcionais em toolbars e linhas de tabela.

**Quando usar `outline`:**

| Contexto | Exemplos |
|----------|----------|
| Grupo de decisão (modal, formulário) | Cancelar, Voltar, Limpar |
| Toolbar de página | Filtros avançados, Exportar, Configurar colunas |
| Ações inline em tabelas/cards | Editar, Duplicar, Visualizar |
| Cabeçalho de seção | "Editar seção", "Ver todos" |

### Ghost — ações mínimas e contextuais

O `ghost` é reservado para ações que não devem chamar atenção — geralmente ícones que aparecem no hover ou em contextos muito densos onde qualquer borda criaria ruído.

**Quando usar `ghost`:**

| Contexto | Exemplos |
|----------|----------|
| Ícone de ação no hover | Lápis ao passar o mouse em um campo ou linha |
| Contextos extremamente densos | Botões de ação dentro de células pequenas |
| Ações de sistema/UI | Fechar, minimizar, expandir |

**Regras do `ghost`:**

- ✅ Quase sempre `size="icon"` — texto em ghost raramente é necessário.
- ✅ Use quando a ação só precisa aparecer no hover, não o tempo todo.
- ❌ Não use `ghost` para ações que o usuário precisa descobrir sem hover — use `outline`.
- ❌ Não use `ghost` no grupo de decisão de modais ou formulários.

## Ícones

O ícone em um botão deve **carregar significado**, não decoração. Se o usuário precisaria ignorar o ícone para ler o texto de qualquer forma, o ícone está gerando ruído.

### Ícone + texto

Use quando o ícone tem uma **metáfora clara e universal** para a ação — ele reduz o tempo de compreensão e ajuda o usuário a escanear múltiplos botões em linha.

| Ação | Ícone recomendado |
|------|------------------|
| Novo / Adicionar | `plus` |
| Editar | `pencil` |
| Excluir | `trash` |
| Exportar / Download | `download` |
| Upload | `upload` |
| Filtros | `sliders-horizontal` |
| Configurar | `settings` |

Também prefira ícone + texto quando há **múltiplos botões outline em linha** — o ícone diferencia visualmente sem precisar ler cada label.

### Somente texto

Use texto puro quando a ação **não tem metáfora visual óbvia** ou quando o ícone seria puramente decorativo:

- Cancelar, Voltar, Limpar
- Salvar rascunho, Publicar
- Confirmar, Continuar, Próximo

Adicionar um ícone genérico nesses casos (ex.: `check` em "Salvar") não reduz o tempo de leitura — só cria ruído.

### Somente ícone (`size="icon"`)

Use apenas quando **todas** as condições forem verdadeiras:

1. O ícone é **universalmente compreendido** (`✕`, `⋮`, `+`, `←`)
2. O **contexto reforça** o significado — `✕` no canto de um modal sempre significa fechar
3. O espaço é **genuinamente limitado** — célula de tabela, toolbar densa
4. O botão tem **`aria-label`** descritivo para acessibilidade

<Warning>
Nunca use `size="icon"` em CTAs primários. O usuário precisa entender a ação antes de clicar — ícone sozinho em `default` gera hesitação.
</Warning>

### Regras de ícone

- ✅ Ícone à **esquerda** do texto — o olho lê ícone antes do label
- ✅ `size="icon"` sempre com `aria-label`
- ❌ Não adicione ícone se ele não reduz o tempo de compreensão
- ❌ Não misture botões com ícone e sem ícone no mesmo grupo de ações — ou todos têm ou nenhum tem

## Tamanhos

<div style={{padding: '24px', background: '#fafafa', border: '1px solid #e5e5e5', borderRadius: '8px', display: 'flex', flexWrap: 'wrap', gap: '8px', alignItems: 'center'}}>
  <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '40px', padding: '0 32px', background: '#1a1a1a', color: '#fafafa', border: 'none', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>lg</button>
  <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', padding: '0 16px', background: '#1a1a1a', color: '#fafafa', border: 'none', borderRadius: '6px', fontSize: '14px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>default</button>
  <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '32px', padding: '0 12px', background: '#1a1a1a', color: '#fafafa', border: 'none', borderRadius: '6px', fontSize: '12px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit'}}>sm</button>
  <button style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'center', height: '36px', width: '36px', background: '#1a1a1a', color: '#fafafa', border: 'none', borderRadius: '6px', fontSize: '16px', cursor: 'pointer', fontFamily: 'inherit'}}>✕</button>
</div>

| Size | Altura | Uso |
|------|--------|-----|
| `lg` | 40px | CTAs de destaque, páginas de onboarding |
| `default` | 36px | Tamanho padrão para a maioria dos contextos |
| `sm` | 32px | Contextos compactos (toolbars, tabelas) |
| `icon` | 36×36px | Botão apenas com ícone (quadrado) |

## Quando usar / quando NÃO usar

- ✅ Ações: salvar, confirmar, enviar, criar, deletar
- ✅ Abertura de modais, drawers, dialogs
- ❌ Navegação entre páginas — use link (`<a>`) ou Link component
- ❌ Ações secundárias de baixa importância — considere `ghost` ou `link` variant

## Regras

- ✅ Máximo de 1 botão `default` (primário) por grupo de ações.
- ✅ Ordem em grupos de decisão: `outline` (esq) → `secondary` (centro) → `default` (dir).
- ✅ `outline` para ações secundárias visíveis: cancelar, editar, filtros avançados, exportar.
- ✅ `ghost` para ações mínimas: ícones de hover, ações em contextos muito densos.
- ✅ Botões destrutivos sempre pedem confirmação via `AlertDialog` antes de executar.
- ✅ Use `size="icon"` com `aria-label` descritivo quando o botão contiver apenas ícone.
- ✅ Estado de loading: desabilite o botão e adicione spinner + texto "Carregando...".
- ❌ Não use `default` variant para ações secundárias.
- ❌ Não coloque o botão `default` à esquerda de outros botões.
- ❌ Não use `ghost` para ações que o usuário precisa descobrir sem passar o mouse — use `outline`.
- ❌ Não use mais de 3 botões no grupo de decisão da mesma linha — consolide em dropdown.

## Exemplos

✅ Correto — cancelar + confirmar (outline → default):
```erb
<%= render RubyUI::Button.new(variant: :outline) { "Cancelar" } %>
<%= render RubyUI::Button.new(variant: :default) { "Salvar" } %>
```

✅ Correto — três níveis em formulário (outline → secondary → default):
```erb
<%= render RubyUI::Button.new(variant: :outline) { "Cancelar" } %>
<%= render RubyUI::Button.new(variant: :secondary) { "Salvar rascunho" } %>
<%= render RubyUI::Button.new(variant: :default) { "Publicar" } %>
```

✅ Correto — ação destrutiva com confirmação:
```erb
<%= render RubyUI::Button.new(variant: :outline) { "Cancelar" } %>
<%= render RubyUI::Button.new(variant: :destructive) { "Excluir fornecedor" } %>
```

✅ Correto — toolbar com outline + CTA primário:
```erb
<%# Lado esquerdo: ações secundárias visíveis %>
<%= render RubyUI::Button.new(variant: :outline, size: :sm) { "Filtros avançados" } %>
<%= render RubyUI::Button.new(variant: :outline, size: :sm) { "Exportar" } %>

<%# Lado direito: ação principal %>
<%= render RubyUI::Button.new(variant: :default) { "Novo fornecedor" } %>
```

✅ Correto — editar inline em tabela (outline):
```erb
<%= render RubyUI::Button.new(variant: :outline, size: :sm) { "Editar" } %>
```

✅ Correto — ação mínima no hover (ghost):
```erb
<%# Aparece apenas no hover da linha %>
<%= render RubyUI::Button.new(variant: :ghost, size: :icon) do %>
  <%= render RubyUI::Icon.new(name: "pencil") %>
<% end %>
```

❌ Errado — ghost para ação que precisa ser descoberta sem hover:
```erb
<%# Usuário não vai encontrar — use outline %>
<%= render RubyUI::Button.new(variant: :ghost, size: :sm) { "Filtros avançados" } %>
```

✅ Correto — ícone + texto em toolbar (metáfora clara):
```erb
<%= render RubyUI::Button.new(variant: :outline, size: :sm) do %>
  <%= render RubyUI::Icon.new(name: "sliders-horizontal") %>
  Filtros avançados
<% end %>
<%= render RubyUI::Button.new(variant: :outline, size: :sm) do %>
  <%= render RubyUI::Icon.new(name: "download") %>
  Exportar
<% end %>
```

✅ Correto — somente texto para ações sem metáfora visual:
```erb
<%= render RubyUI::Button.new(variant: :outline) { "Cancelar" } %>
<%= render RubyUI::Button.new(variant: :default) { "Salvar rascunho" } %>
```

❌ Errado — ícone decorativo sem metáfora clara:
```erb
<%# "check" em "Confirmar" não reduz compreensão — só adiciona ruído %>
<%= render RubyUI::Button.new(variant: :default) do %>
  <%= render RubyUI::Icon.new(name: "check") %>
  Confirmar
<% end %>
```

❌ Errado — mistura de botões com e sem ícone no mesmo grupo:
```erb
<%= render RubyUI::Button.new(variant: :outline) { "Cancelar" } %>
<%= render RubyUI::Button.new(variant: :default) do %>
  <%= render RubyUI::Icon.new(name: "save") %>
  Salvar
<% end %>
```

❌ Errado — dois botões primários concorrendo:
```erb
<%= render RubyUI::Button.new(variant: :default) { "Salvar" } %>
<%= render RubyUI::Button.new(variant: :default) { "Publicar" } %>
```

❌ Errado — usar Button para navegação:
```erb
<%= render RubyUI::Button.new(href: "/suppliers") { "Ver fornecedores" } %>
```
